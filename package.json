{
  "name": "project-tracker",
  "version": "1.0.0",
  "description": "A comprehensive project tracking application for managing projects, resources, financials, and milestones",
  "main": "dist/server.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd src/backend && npm run dev",
    "client:dev": "cd src/frontend && npm run dev",
    "build": "npm run server:build && npm run client:build",
    "server:build": "cd src/backend && npm run build",
    "client:build": "cd src/frontend && npm run build",
    "start": "cd src/backend && npm start",
    "test": "npm run server:test && npm run client:test",
    "server:test": "cd src/backend && npm test",
    "client:test": "cd src/frontend && npm test",
    "setup": "npm install && cd src/backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["project-tracking", "resource-management", "financial-tracking", "milestone-management"],
  "author": "Project Tracker Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}